<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Authentification biométrique -->
    <uses-permission android:name="android.permission.USE_BIOMETRIC" />
    <!-- Notifications pour les rappels budgétaires -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <!-- Planification des rappels en arrière-plan -->
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM" />
    <!-- Taux de change live (API Frankfurter) -->
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".DibitaraApp"
        xmlns:tools="http://schemas.android.com/tools"
        android:allowBackup="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Dibitara">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
            <!-- Deep link depuis les notifications : dibitara://settings -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <data android:scheme="dibitara" android:host="settings" />
            </intent-filter>
        </activity>

        <!--
            FileProvider sécurise le partage des fichiers d'export (CSV/JSON).
            android:exported="false" + android:grantUriPermissions="true" =
            seul l'app destinataire reçoit un accès temporaire à l'Uri.
        -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.provider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_provider_paths" />
        </provider>

        <!--
            Désactive l'initialisation automatique de WorkManager pour permettre
            l'injection de HiltWorkerFactory via Configuration.Provider dans DibitaraApp.
        -->
        <provider
            android:name="androidx.startup.InitializationProvider"
            android:authorities="${applicationId}.androidx-startup"
            android:exported="false"
            tools:node="merge">
            <meta-data
                android:name="androidx.work.WorkManagerInitializer"
                tools:node="remove" />
        </provider>

    </application>

</manifest>
